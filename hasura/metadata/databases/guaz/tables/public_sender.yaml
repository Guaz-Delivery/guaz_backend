table:
  name: sender
  schema: public
array_relationships:
  - name: sender_OTPs
    using:
      foreign_key_constraint_on:
        column: sender_id
        table:
          name: sender_OTP
          schema: public
